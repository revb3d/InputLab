{
  "version": "1.3.48",
  "download_url": "https://github.com/revb3d/InputLab/releases/download/v1.3.48/InputLabSetup.exe",
  "notes": [
    "Fixed controller profile tab selection to use stable profile IDs instead of names.",
    "Prevented generic UI sync from silently persisting duplicate profile names or hotkeys during actions like Start, Add Profile, or tab switching.",
    "This fixes cases where editing a new profile could appear to overwrite or duplicate other profiles."
  ]
}
